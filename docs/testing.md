# Testing Guide

- Install dependencies: `pnpm install`
- Run unit and integration tests: `pnpm test`
- Run coverage suite: `pnpm run test:coverage`
- Lint: `pnpm run lint`

Coverage thresholds are enforced at 90% for branches, functions, lines, and statements.
